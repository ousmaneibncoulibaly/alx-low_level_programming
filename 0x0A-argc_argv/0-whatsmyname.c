#include "main.h"
/**
 * main - a programm that can print it's name.
 * @argc: argument counter.
 * @argv: argument viewer.
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
if (argc > 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
