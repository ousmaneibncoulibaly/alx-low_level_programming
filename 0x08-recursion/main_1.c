#include "main.h"
#include <stdio.h>

/**
 * main - check the code for ALX School students.
 *
 * Return: Always 0.
 */
int main(void)
{
int r;
r = is_palindrome("abcdcba");
printf("%d\n", r);
return (0);
}
